# === 💎 Inspiro AI · Railway Nixpacks（v4.3 穩定版）===
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install --omit=dev"]

[phases.build]
cmds = ["echo '🚀 Inspiro AI v4.3 build complete.'"]

[start]
cmd = "npm start"
