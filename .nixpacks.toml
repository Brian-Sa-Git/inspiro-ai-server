# === Inspiro AI · Railway Nixpacks 設定 ===
# 建立 Node 18+ 環境，並自動安裝依賴與啟動伺服器

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo '✅ Inspiro AI build complete'"]

[start]
cmd = "npm start"
